/// Shared layout constants (aligned with Care System onboarding design).
const double kLeftRightMargin = 16.0;

/// Responsive breakpoints for mobile / tablet / desktop.
/// - Mobile: < 600 (phone, single column, bottom nav).
/// - Tablet: 600–900 (optional side rail or still bottom nav).
/// - Desktop: >= 900 (side nav rail, max-width content).
const double kBreakpointMobile = 600;
const double kBreakpointDesktop = 900;

/// Max content width for web/desktop so content doesn't stretch too wide.
const double kMaxContentWidth = 1200;
const double kMaxFormWidth = 440;
